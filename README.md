# gnome-shell-extension-toggle-workspace-span
Gnome shell extension to show a button on the panel which toggles between the settings "Workspaces only on primary display only" and "Workspaces span displays".
